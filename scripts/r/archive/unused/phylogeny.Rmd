

```{r}
# TODO: import libraries
# TODO: load phyloseq object

myTaxa = names(sort(taxa_sums(ps), decreasing = TRUE)[1:10])
ex1 = prune_taxa(myTaxa, ps)
plot(phy_tree(ex1), show.node.label = TRUE)
plot_tree(
  ex1,
  color = "cultivar",
  label.tips = "Phylum",
  ladderize = "left",
  justify = "left" ,
  size = "Abundance"
)

hist(
  sample_sums(ps),
  main = "Read Counts",
  xlab = "Total Reads",
  border = "black",
  col = "orange",
  las = 1,
  breaks = 12
)
```

